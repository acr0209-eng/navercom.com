// 실험 전역 설정 — 제안서 4장(PFI) 기준값과 가중치를 한 곳에서 관리한다.

export const CONFIG = {
  // 과업당 제한 시간(초). 초과 시 실패로 기록된다.
  taskTimeLimitSec: 180,

  // 토글 직후 '실행 취소' 스낵바 노출 시간(ms). 제안서 3.2절: 5초.
  snackbarMs: 5000,

  // PFI 정규화 기준(고정 기준 정규화).
  // 각 지표를 0~1로 정규화한 뒤 가중합 × 100 = PFI (0~100, 낮을수록 마찰이 적다).
  // 소규모 패널에서는 z-표준화가 불안정하므로(표본 2~20), 고정 상한 기준을 쓴다.
  // 상한을 넘는 값은 1로 캡. 기준값은 README에 문서화되어 있다.
  pfi: {
    caps: {
      clicks: 15,        // 클릭 수 상한
      screens: 10,       // 화면 전환(뎁스) 상한
      durationSec: 180,  // 소요 시간 상한 = 제한 시간
    },
    // 5개 지표 가중치(합 1). 제안서 4.1절의 5개 지표.
    weights: {
      clicks: 0.2,       // 클릭 수
      screens: 0.2,      // 경로 뎁스
      durationSec: 0.2,  // 소요 시간
      fail: 0.2,         // 과업 실패(성공률의 시행 단위 반영, 0/1)
      seq: 0.2,          // 주관적 난이도 (1=매우 쉬움 ~ 7=매우 어려움)
    },
  },

  // localStorage 키
  storageKey: 'pfi_sessions_v1',
};

export const CONDITIONS = {
  A: { id: 'A', name: '현행 목적지형', desc: '설정 메뉴를 찾아가는 현행 방식 시뮬레이션' },
  B: { id: 'B', name: '제안 접점형(내 정보 영수증)', desc: '사용 접점의 영수증에서 바로 통제' },
};
